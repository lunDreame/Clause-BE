# LLM 설정
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=your-api-key-here
LLM_MODEL=gpt-4o-mini
LLM_TIMEOUT_MS=10000
LLM_MAX_RETRY=2

# 데이터베이스 설정 (프로덕션)
# DATABASE_URL=jdbc:postgresql://localhost:5432/clause
# DATABASE_USERNAME=clause
# DATABASE_PASSWORD=your-password

# CORS 설정 (프로덕션)
# CORS_ALLOWED_ORIGINS=https://clause.example.com

# Rate Limit 설정
# RATE_LIMIT_PER_MINUTE=30
